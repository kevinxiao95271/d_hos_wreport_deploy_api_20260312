<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kxhospital.wreport.mapper.WrRecordMapper">

    <select id="selectAdminPage" resultType="com.kxhospital.wreport.entity.WrRecord">
        SELECT * FROM wr_record
        WHERE del_flag = 0
        <if test="taskId != null">
            AND task_id = #{taskId}
        </if>
        <if test="orgName != null and orgName != ''">
            AND org_name LIKE CONCAT('%', #{orgName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectMyPage" resultType="com.kxhospital.wreport.entity.WrRecord">
        SELECT * FROM wr_record
        WHERE del_flag = 0 AND org_id = #{orgId}
        <if test="taskId != null">
            AND task_id = #{taskId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectAggregate" resultType="java.util.HashMap">
        SELECT
            COUNT(1) AS total,
            SUM(CASE WHEN status = 0 THEN 1 ELSE 0 END) AS draft,
            SUM(CASE WHEN status = 1 THEN 1 ELSE 0 END) AS submitted,
            SUM(CASE WHEN status = 2 THEN 1 ELSE 0 END) AS approved,
            SUM(CASE WHEN status = 3 THEN 1 ELSE 0 END) AS rejected,
            SUM(CASE WHEN reject_apply_status = 1 THEN 1 ELSE 0 END) AS pendingRejectApply
        FROM wr_record
        WHERE del_flag = 0
        <if test="taskId != null">
            AND task_id = #{taskId}
        </if>
    </select>

</mapper>
