<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kxhospital.wreport.mapper.WrRecordMapper">

    <select id="selectAdminPage" resultType="com.kxhospital.wreport.entity.WrRecord">
        SELECT * FROM wr_record
        WHERE del_flag = 0
        <if test="taskId != null">
            AND task_id = #{taskId}
        </if>
        <if test="orgName != null and orgName != ''">
            AND org_name LIKE CONCAT('%', #{orgName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

</mapper>
