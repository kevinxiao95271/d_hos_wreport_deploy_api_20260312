<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kxhospital.wreport.mapper.WrTaskOrgScopeMapper">

    <insert id="insertBatch">
        INSERT INTO wr_task_org_scope(id, task_id, org_id, create_user, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.taskId}, #{item.orgId}, #{item.createUser}, #{item.createTime})
        </foreach>
    </insert>

</mapper>
