<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kxhospital.wreport.mapper.WrRecordValueMapper">

    <!-- PostgreSQL ON CONFLICT upsert -->
    <insert id="batchUpsert">
        INSERT INTO wr_record_value (id, record_id, template_id, item_id, row_index, cell_value, create_user, create_time, update_time)
        VALUES
        <foreach collection="list" item="v" separator=",">
            (#{v.id}, #{v.recordId}, #{v.templateId}, #{v.itemId}, #{v.rowIndex}, #{v.cellValue},
             #{v.createUser}, #{v.createTime}, #{v.updateTime})
        </foreach>
        ON CONFLICT (record_id, item_id, row_index)
        DO UPDATE SET cell_value = EXCLUDED.cell_value, update_time = EXCLUDED.update_time
    </insert>

</mapper>
